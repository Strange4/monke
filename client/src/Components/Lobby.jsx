import './Styles/Lobby.css';

/**
 * Displays a Popup for the lobby
 * @returns {ReactElement}
 */
function Lobby() {
    return (
        <div id="lobby">
            <h1>Lobby Popup</h1>
        </div>
    );
}

export default Lobby;